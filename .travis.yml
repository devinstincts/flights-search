language: node_js
node_js:
  - lts/carbon
env:
  global:
    - NPM_CONFIG_LOGLEVEL=warn
cache:
  directories:
    - "node_modules"
branches:
  only:
    - master
script:
    - npm install:all
    - npm lint:all
    - npm test:all
    - npm build
