language: node_js
node_js:
  - lts/carbon
env:
  global:
    - NPM_CONFIG_LOGLEVEL=warn
cache:
  directories:
    - "node_modules"
branches:
  only:
    - master
script:
    - npm run install:all
    - npm run lint:all
    - npm run test:all
    - npm run build:all
